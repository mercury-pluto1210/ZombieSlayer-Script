/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuStopSound : MonoBehaviour
{
    GameObject settingWindowUI;
    GameObject gunshotSound;
    GunShotSound gunShotSoundScript;

    // Start is called before the first frame update
    void Start()
    {
        settingWindowUI = GameObject.Find("SettingWindowUI");
        gunshotSound = GameObject.Find("GunShotSound");
        gunShotSoundScript = gunshotSound.GetComponent<GunShotSound>();
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetButtonDown(0))
        {
            //gunShotSoundScript.enabled = false;
        }
        else
        {
            //gunShotSoundScript.enabled = true;
        }
    }
}*/
